// Crea un programa que muestre en la consola la tabla de multiplicar del 5 (desde 1 hasta
//     10) utilizando un bucle `for`.

let valor = 5
for (let i = 1; i <= 10; i++) {
    console.log(`${valor}x${i}=${valor*i}`);
}